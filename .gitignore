# ==============================================================================
# 🏛️ AOXCDAO INSTITUTIONAL GIT EXCLUSION POLICY
# 🛡️ SCOPE: Solidity 0.8.33 | Foundry Framework | Enterprise Security
# 🎓 LEVEL: Pro Ultimate Academic
# ==============================================================================

# --- 🏗️ COMPILATION & BUILD ARTIFACTS ---
# Local build outputs and cached metadata
out/
cache/
artifacts/
typechain/
typechain-types/

# --- 📦 DEPENDENCY MANAGEMENT ---
# External libraries are managed via git submodules or specific managers
lib/
node_modules/
# Keep forge-std for local consistency if necessary
# !lib/forge-std

# --- 🔐 CRYPTOGRAPHIC & SENSITIVE DATA (CRITICAL) ---
# Strict exclusion of private keys, environment variables, and mnemonics
.env
.env.*
secrets.json
*.pem
*.key
mnemonic.txt
*password.txt
# Foundry deployment broadcast data (contains private key metadata)
broadcast/

# --- 🧪 ANALYSIS & TELEMETRY ---
# Local testing coverage, gas metrics, and audit tool artifacts
coverage/
lcov.info
gas-report.txt
.gas-snapshot
slither-reports/
aderyn-report.json
*.log
slither.db.json

# --- 🛠️ IDE & WORKSPACE CONFIGURATION ---
# Preventing local environment leakage into global history
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
.project
.settings/
.classpath

# --- 🐍 TOOLING & SCRIPTING (Python/Node) ---
# Artifacts from auxiliary scripts or monitoring tools
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.venv/
.env/

# --- 🖥️ OPERATING SYSTEM ARTIFACTS ---
.DS_Store
Thumbs.db
.Spotlight-V100
.Trashes

# --- ♻️ TEMPORARY & RECOVERY FILES ---
*.tmp
*.bak
*.old
*.orig
.cache/

# ==============================================================================
# 📜 AOXCDAO QUALITY GATE
# This exclusion policy ensures that sensitive keys and local build noise 
# never pollute the institutional ledger.
# ==============================================================================
