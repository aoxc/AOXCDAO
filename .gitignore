# ==============================================================================
# 🏛️ AOXCDAO INSTITUTIONAL GIT EXCLUSION POLICY V5.0
# 🛡️ ARCHITECTURE: Foundry Submodules / Yarn Berry / Hardhat Integration
# 🎓 LEVEL: Pro Ultimate Academic (Enterprise Grade / 2026 Secured)
# ==============================================================================

# --- 🏗️ COMPILATION & BUILD ARTIFACTS ---
# Eliminating all deterministic build leftovers
out/
cache/
artifacts/
typechain/
typechain-types/
cache_hardhat/
target/

# --- 📦 DEPENDENCY MANAGEMENT (YARN BERRY / NPM) ---
# Enforcing 'Zero-Install' stability while ignoring bloat
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
!.yarn/install-state.gz

# --- 🧶 YARN SPECIFIC NOISE ---
yarn-error.log
yarn-debug.log*
.eslintcache
.stylelintcache
*.tsbuildinfo

# --- 🧪 FOUNDRY & ANALYSIS ARTIFACTS ---
# Pro-Tip: We do NOT ignore 'lib/' entirely because AOXCDAO uses submodules.
# We only ignore potential junk inside dependencies.
lib/*/.git
lib/*/.github
lib/*/test
lib/*/spec

# Foundry specific snapshots and logs
.gas-snapshot
.forge-snapshot
gas-report.txt
coverage/
lcov.info

# --- 🔐 CRYPTOGRAPHIC & SENSITIVE DATA (CRITICAL) ---
# Zero-tolerance policy for private key leaks
.env
.env.*
.secrets
secrets.json
*.pem
*.key
mnemonic.txt
*password.txt
# Critical: Forge broadcast logs contain deployment private keys/addresses
broadcast/

# --- 🔍 STATIC ANALYSIS & SECURITY AUDIT ---
# Ignoring local scan databases and temporary reports
slither-reports/
slither.db.json
aderyn-report.json
mythril_reports/
corpus/
*.log

# --- 🛠️ IDE & WORKSPACE CONFIGURATION ---
# Preserving team extensions while ignoring user-specific state
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.project
.settings/
.classpath

# --- 🖥️ OPERATING SYSTEM ARTIFACTS ---
.DS_Store
Thumbs.db
.Spotlight-V100
.Trashes

# --- ♻️ TEMPORARY & RECOVERY FILES ---
*.tmp
*.bak
*.old
*.orig
.cache/
.foundry/

# ==============================================================================
# 📜 AOXCDAO QUALITY GATE
# VERSION: 5.0.0 (Institutional Grade)
# STATUS: Fully compliant with 0.8.33 Hardhat/Foundry Hybrid Architecture.
# ==============================================================================
